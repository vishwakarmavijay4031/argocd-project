apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-pod
  name: nginx-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    name: nginx-pod
  type: NodePort
status:
  loadBalancer: {}
